package com.datasphere.hdstore.constants;

public enum Capability
{
    SELECT, 
    INSERT, 
    DELETE, 
    PROJECTIONS, 
    LIMIT, 
    FROM, 
    FROM_MULTIPLE, 
    WHERE, 
    ORDER_BY, 
    GROUP_BY, 
    GROUP_BY_NULL_VALUES, 
    HAVING, 
    LIKE, 
    REGEX, 
    BETWEEN, 
    IN_SET, 
    TO_LOWER, 
    TO_UPPER, 
    LENGTH, 
    ISNULL, 
    AGGREGATION_MIN, 
    AGGREGATION_MAX, 
    AGGREGATION_COUNT, 
    AGGREGATION_SUM, 
    AGGREGATION_AVG, 
    AGGREGATION_DISTINCT, 
    AGGREGATION_FIRST, 
    AGGREGATION_LAST, 
    DATA_ID, 
    DATA_TIMESTAMP, 
    DATA_TIME_TO_LIVE, 
    DATA_ROUTING, 
    DATA_ANY, 
    INDEX, 
    UNIQUE_INDEX, 
    NON_UNIQUE_INDEX, 
    CLUSTERED_INDEX, 
    PARTITIONING_INDEX, 
    QUERY_FILTER_REQUIRED, 
    QUERY_PK_FIELDS_REQUIRED, 
    QUERY_PK_FIELDS_PARTIAL, 
    QUERY_NON_PK_FIELDS;
}
